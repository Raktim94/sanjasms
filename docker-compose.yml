services:
  cms:
    build: .
    container_name: odoo-lite-cms
    ports:
      - "8080:8080"
    volumes:
      - cms_data:/data
      - ./static/uploads:/app/static/uploads
    environment:
      - DB_Path=/data/cms.db
    restart: always

volumes:
  cms_data:
