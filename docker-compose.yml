services:
  cms:
    build: .
    container_name: sanjanacms
    ports:
      - "8090:8080"
    volumes:
      - cms_data:/data
      - ./static/uploads:/app/static/uploads
    environment:
      - DB_Path=/data/cms.db
    restart: always

volumes:
  cms_data:
